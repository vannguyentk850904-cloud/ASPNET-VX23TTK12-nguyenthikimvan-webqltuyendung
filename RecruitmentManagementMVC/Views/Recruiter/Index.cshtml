@{
    ViewBag.Title = "Recruiter Dashboard";
}

<div class="container mt-5 text-center">
    <h2 class="text-success">Khu vực Nhà Tuyển Dụng</h2>
    <p class="text-muted">Đăng tin tuyển dụng và quản lý các ứng viên tại đây.</p>
    <a href="@Url.Action("Create", "JobPosts")" class="btn btn-primary mt-3">Đăng tin mới</a>
</div>
